pypypy

import prody (parse), pyrosetta(__?__), 3d-coords, 



def res-to-stub(res1, res2):
    match res-s to atoms
    return alpha, beta, gamma (of each stub)

def stub-shape():
    bend1 = angle(alpha.beta.gamma)
    bend2 = angle(alpha.beta.gamma)
    return bends

def transform():
    3d-coords to euler coords
    return (angle,angle,angle)
    relational vector between alpha carbons
    return vector


def main(pdb id, res1, res2):

    check input
    if not sanitary:
        print "usage: pdb (file or id), res1, res2"
    else:
        print "response"
        download if id

    parse for atoms
    --> xyz coords 

    res-to-stub 
    --> 2x 3-atom-stubs
    stub shape
    --> one angle each

    transform
    --> euler coords and vector

    compare to pdb2

    
